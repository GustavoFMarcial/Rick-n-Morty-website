<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>A Rick and Morty Website</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
</head>

<body>

    <div class="container">
        <div class="container-content">
            <!-- Condição onde a request não foi feita -->
            <% if (!locals.data) { %>
                <img class="question-mark-img" src="/images/question-mark.jpg">
                <p>Selecione um personagem para ver suas informações</p>
            <% } %>
            <!-- Condição onde a request foi feita e retornou 200 -->
            <% if (locals.data) { %>
                <div class="container-information">
                    <img class="character-img" src="<%= data[5].data %>">
                        <ul>
                            <% for (let i = 0; i < data.length - 1; i++) { %>
                                <li><%= data[i].data %></li>
                            <% } %>
                        </ul>
                </div>
            <% } %>
            <!-- Condição onde a request foi feita e retornou algum erro -->
            <% if (locals.error) { %>
                <%= error %>
            <% } %>
        </div>
        <!-- Formulário para o usuário selecionar o personagem que deseja ver as informações -->
        <div class="container-submit">
            <form method="POST" action="/submit">
                <label for="characters"></label>
                    <select class="select" id="characters" name="id">
                        <option value="1">Rick Sanchez</option>
                        <option value="2">Morty Smith</option>
                        <option value="3">Summer Smith</option>
                        <option value="4">Beth Smith</option>
                        <option value="5">Jerry Smith</option>
                        <option value="6">Abadango Cluster Princess</option>
                        <option value="7">Abradolf Lincler</option>
                        <option value="8">Adjudicator Rick</option>
                        <option value="9">Agency Director</option>
                        <option value="10">Alan Rails</option>
                    </select>
                <input class="button" type="submit">
            </form>
        </div>
    </div>
    
</body>

</html>